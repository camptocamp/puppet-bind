# File managed by puppet
zone <%= @name %> IN {
  type slave;
  <% if @zone_masters.is_a?(Array) -%>
  masters { <%= @zone_masters.join('; ') -%>; };
  <% else -%>
  masters { <%= @zone_masters %>; };
  <% end -%>
  allow-query { any; };
};
